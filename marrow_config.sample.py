import os
import base64

class config:
    debug = True
    secret_key = "<some random string>"
    db = "<theDatabaseName>"
    user = "<theDatabaseUser>"
    password = "<theDatabasePassword>"
    host = "localhost"

